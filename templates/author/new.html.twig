{% extends 'base.html.twig' %}

{% block title %}New Author — AuditorBundle Demo{% endblock %}

{% block body %}
<a href="{{ path('app_author_index') }}" class="back-link">← Authors</a>

<div class="max-w-2xl">
    <h1 class="page-title mb-6">New Author</h1>
    <div class="card">
        <div class="card-body">
            {{ form_start(form, {attr: {class: 'space-y-4'}}) }}
                {% for field in form %}
                    <div>
                        {{ form_label(field, null, {label_attr: {class: 'form-label'}}) }}
                        {{ form_widget(field, {attr: {class: field.vars.block_prefixes|last == 'textarea' ? 'form-textarea' : 'form-input'}}) }}
                        {{ form_errors(field) }}
                    </div>
                {% endfor %}
                <hr class="divider">
                <div class="flex gap-3">
                    <button type="submit" class="btn btn-primary">Save Author</button>
                    <a href="{{ path('app_author_index') }}" class="btn btn-secondary">Cancel</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
