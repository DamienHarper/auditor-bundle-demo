{% extends 'base.html.twig' %}

{% block title %}Edit Tag — AuditorBundle Demo{% endblock %}

{% block body %}
<a href="{{ path('app_tag_index') }}" class="back-link">← Tags</a>

<div class="max-w-md">
    <h1 class="page-title mb-6">Edit Tag: {{ tag.name }}</h1>
    <div class="card">
        <div class="card-body">
            {{ form_start(form, {attr: {class: 'space-y-4'}}) }}
                {% for field in form %}
                    <div>
                        {{ form_label(field, null, {label_attr: {class: 'form-label'}}) }}
                        {{ form_widget(field, {attr: {class: 'form-input'}}) }}
                        {{ form_errors(field) }}
                    </div>
                {% endfor %}
                <hr class="divider">
                <div class="flex gap-3">
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                    <a href="{{ path('app_tag_index') }}" class="btn btn-secondary">Cancel</a>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
